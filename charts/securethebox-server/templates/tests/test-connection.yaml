apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "securethebox-server.fullname" . }}-test-connection"
  labels:
{{ include "securethebox-server.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "securethebox-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
